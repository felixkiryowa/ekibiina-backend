spring.application.name=ekibiina
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.xml
spring.liquibase.enabled=true

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ekibiina_db
spring.datasource.username=postgres
spring.datasource.password=password123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=eCkSL+XbqpZPgkTG55ntcxDRmRDSR8b/e+vbzoSE+xI=
# 15 minutes (900 seconds) for access tokens
jwt.expiration=900

# If using refresh tokens (recommended), set longer expiration for them (e.g., 7 days)
jwt.refreshExpiration=604800

logging.level.org.springframework.security=DEBUG